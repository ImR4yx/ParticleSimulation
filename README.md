Project contains particle simulations with different types of collision algorithms.
-slow_algorithm.py contains an o(n^2) agorithm
-spatial_partitioning_algorithm contains the same simulation but with an spatial partitioning alogrithm which greatly improves performance of the simulation
-main_menu contains the code that is used to display the main menu at startup